# Optional overrides for respira-certbot-renew.service
# Copy to /etc/default/respira-certbot-renew

# Compose project path on the host
RESPIRA_COMPOSE_PATH=/workspaces/respira-webapp

# Optional log file override for renewal runs
# RESPIRA_CERTBOT_LOG_FILE=/var/log/respira-certbot.log
